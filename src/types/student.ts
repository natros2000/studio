export interface Student {
  id: string;
  nombre: string;
  apellido: string;
  cedula: string;
  fechaNacimiento: string; // ISO String
  direccion: string;
  instrumento: string;
  fechaInscripcion: string; // ISO String
}
